<Window x:Class="Gestor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        xmlns:local="clr-namespace:Gestor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="0 0 10 0" BorderThickness="25" Height="100" Width="100" Click="Button_Click">
                <TextBlock>Add User</TextBlock>
            </Button>
            <Button Margin="0 0 10 0"  BorderThickness="25" Height="100" Width="100">
                <TextBlock>Add User</TextBlock>
            </Button>
            <Button BorderThickness="25" Height="100" Width="100">
                <TextBlock>Add User</TextBlock>
            </Button>
        </StackPanel>
    </Grid>
</Window>
